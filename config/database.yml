default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch('RAILS_MAX_THREADS') { 5 } %>

development:
  <<: *default
  database: crypto_index_home_development

test:
  <<: *default
  database: crypto_index_home_test
